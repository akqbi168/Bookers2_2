<div class="container">
  <div class='row'>
    <div class='col-md-6 col-md-offset-3'>
      <h1>Editing Book</h1>

      <!-- layouts/application.html.erb で設定済みなのでコメントアウト
        <% if @book.errors.any? %>
          <div class="alert-danger">
            <strong>
              <p><%= @book.errors.count %> error: cannot be saved</p>
              <ul>
                <% @book.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
                <% end %>
              </ul>
            </strong>
          </div>
        <% end %>
       -->

      <%= render 'books/newbook', book: @book %>

      <%= link_to 'Show', @book, class: "book_#{@book.id}" %>
       | <%= link_to 'Back', books_path, class: "back" %>

    </div>
    <div class='col-md-3'>
    </div>
  </div>
</div>